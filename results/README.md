# Results

This directory is reserved for small, paper-facing tables, figures, source data,
and selected-run metadata. Raw run directories and checkpoints are intentionally
excluded from version control.

No provisional numeric results are published here. Frozen artifacts should be
added only after the complete experiment and consistency audits pass.

